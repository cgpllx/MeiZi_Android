package com.meizitu.core;

/**
 * Is it mandatory to take data from the network after the control error
 */
public interface CacheControl {
    String FORCE_NETWORK = "no-cache";
    String DEFAULT = "";
}
